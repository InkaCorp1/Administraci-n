/**
 * Módulo de Aportes Móvil (Versión Express)
 */

async function initAportesModule() {
    console.log('Aportes module loaded (Maintenance Mode)');
    // Aquí se cargará la lógica de aportes en el futuro
}

// Global exposure
window.initAportesModule = initAportesModule;
